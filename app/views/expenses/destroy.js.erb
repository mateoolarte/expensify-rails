// remove the row from list
$(`#${<%= @expense.id %>}`).remove()

// update header values
$('.total-per-month').html("<%= number_to_currency(get_total_amount_per_month(@expenses_per_month)) %>")
$('.total-transactions').html("<%= @expenses_per_month.count %>")
$('.average-transactions').html("<%= number_to_currency(get_average_per_month(@expenses_per_month)) %>")

// show alert
$(".expense-alert").removeClass("alert-success").addClass("fade in alert-danger")

const type = "<%= flash[:delete].options %>"
const concept = "<%= flash[:delete].concept %>"
const amount = "<%= number_to_currency(flash[:delete].amount) %>"
const date = "<%= flash[:delete].date.strftime("%B %d") %>"

$(".expense-alert div").html(`${type} <strong>${concept}</strong> por <strong>${amount}</strong> el día <strong>${date}</strong> fue eliminado exitosamente`)